server:
  port: 8300
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: oauth-service
  sleuth:
    sampler:
      probability: 0.05
  #zipkin:
    #base-url: http://localhost:9411/
springdoc:
  api-docs:
    path: oauth-service/v3/api-docs